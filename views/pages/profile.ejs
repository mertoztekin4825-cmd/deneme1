<section class="profile">
  <h1>Profilim</h1>
  <div class="profile-card">
    <p><strong>Ad Soyad:</strong> <%= profile.name %></p>
    <p><strong>E-posta:</strong> <%= profile.email %></p>
    <p><strong>Üyelik Tarihi:</strong> <%= formatDate(profile.created_at) %></p>
  </div>
</section>
